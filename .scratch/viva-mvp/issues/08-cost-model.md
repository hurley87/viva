# Cost model

Type: research
Status: open

## Question

Build the arithmetic the INV-4 caps currently assume: current OpenAI Realtime audio pricing per 15-min Session (both directions + text), Grader pass cost, classifier pass cost → per-Session and per-Student-week cost, parameterized by class size (unknown yet). Validate the default caps (2/day, 8/week, 15-min box) against the $5K grant and propose adjusted ceilings if they don't fit.

Deliberately deferred per the charting grill ("not something we'll do up front") — on the frontier but low priority; fire when wanted.
